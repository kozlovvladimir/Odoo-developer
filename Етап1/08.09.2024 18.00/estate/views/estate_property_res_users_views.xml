<odoo>
    <record id="view_users_form_inherited_estate" model="ir.ui.view">
        <field name="name">res.users.form.inherited.estate.property</field>
        <field name="model">res.users</field>
        <field name="inherit_id" ref="base.view_users_form"/>
        <field name="arch" type="xml">
            <!-- Додаємо нову вкладку в блок notebook форми користувача -->
            <xpath expr="//sheet/notebook" position="inside">
                <page string="Real Estate Properties">
                    <!-- Додаємо поле property_ids для відображення доступних об'єктів нерухомості -->
                    <field name="property_ids" domain="[('state', 'in', ['new', 'offer_received'])]">
                        <tree string="Available Properties">
                            <field name="name" string="Title"/>
                            <field name="property_type_id" string="Property Type"/>
                            <field name="postcode" string="Postcode"/>
                            <field name="tag_ids" widget="many2many_tags" string="Tags"/>
                            <field name="bedrooms" string="Bedrooms"/>
                            <field name="living_area" string="Living Area (sqm)"/>
                            <field name="expected_price" string="Expected Price"/>
                            <field name="selling_price" string="Selling Price"/>
                            <field name="state" string="Status"/>
                        </tree>
                    </field>
                </page>
            </xpath>
        </field>
    </record>
</odoo>
